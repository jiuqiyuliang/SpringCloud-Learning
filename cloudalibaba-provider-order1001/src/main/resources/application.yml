server:
  port: 1001



spring:
  application:
    name: nacos-order-provider
  cloud:
    nacos:
      discovery:
        server-addr: localhost

management:
  endpoints:
    web:
      exposure:
        include: '*'